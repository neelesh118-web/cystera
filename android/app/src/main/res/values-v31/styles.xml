<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12 and above draw their own splash screen, and they read it from here
  rather than from `windowBackground`. Without these three items the platform would
  put the *launcher icon* in a white circle in the middle of the screen instead —
  which on this app is a rose tile on a white field, the opposite of the launch the
  rest of this app is built to give.

  `windowSplashScreenBackground` takes a colour and not a drawable, which is the one
  place the ramp cannot follow: pre-12 gets the gradient, this gets its first stop.
  The two read as the same screen because the mark is identical and the crimson is
  where the ramp begins.
-->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_mark</item>
        <!-- Nothing moves: the app is not announcing itself, it is arriving. -->
        <item name="android:windowSplashScreenAnimationDuration">0</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/app_background</item>
    </style>
</resources>
