<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Dark mode keeps the same launch ramp: the app's dark theme takes its gradient
      from the same two rose tokens, so the splash is identical in both and there is
      nothing to override. Only the colour behind the Flutter surface changes, to the
      dark theme's own background (`Rose.ink`).
    -->
    <color name="app_background">#0B0709</color>
</resources>
